# Start Here

1. `pip install -e ".[dev]"`
2. `pytest`
3. Configure AWS credentials and Bedrock model access.
4. `cp .env.example .env`
5. Start the API and compare BM25, TF-IDF, and hybrid evaluation runs.
